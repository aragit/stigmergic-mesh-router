apiVersion: v1
kind: Service
metadata:
  name: {{ include "stigmergic-mesh-router.fullname" . }}
  labels:
    {{- include "stigmergic-mesh-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "stigmergic-mesh-router.selectorLabels" . | nindent 4 }}
